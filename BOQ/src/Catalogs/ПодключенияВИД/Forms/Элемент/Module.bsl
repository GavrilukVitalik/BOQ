
&НаСервере
Процедура ПриСозданииНаСервере ( Отказ, СтандартнаяОбработка )
	
	заполнитьМассивВИД ();

КонецПроцедуры

&НаСервере
Процедура заполнитьМассивВИД ()
	
	м = ВИД.ПолучитьМассивИсточников ();
	Элементы.ИмяВнешнегоИсточника.СписокВыбора.ЗагрузитьЗначения ( м );
	Если ( Объект.ИмяВнешнегоИсточника <> "" ) Тогда
		Элементы.ИмяВнешнегоИсточника.СписокВыбора.НайтиПоЗначению ( Объект.ИмяВнешнегоИсточника );
	КонецЕсли;
			
КонецПроцедуры