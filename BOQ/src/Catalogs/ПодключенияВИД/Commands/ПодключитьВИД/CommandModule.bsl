
&НаКлиенте
Процедура ОбработкаКоманды ( ПараметрКоманды, ПараметрыВыполненияКоманды )

	ответ = ВИД.ПодключитьКВИД ( ПараметрКоманды );
	Если ( ответ ) Тогда
		Сообщения.ПредупреждениеПоказать ( "ПодключениеКВИДВыполнено" );
	Иначе
		Сообщения.ПредупреждениеПоказать ( "ПодключениеКВИДНеВыполнено" );		
	КонецЕсли;

КонецПроцедуры